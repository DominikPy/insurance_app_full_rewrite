<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM"
      crossorigin="anonymous"
    />

    <title>Document</title>
  </head>
  <body>
    <nav class="navbar navbar-expand-md navbar-dark bg-dark">
      <div class="container">
        <a href="http://localhost:3000" class="navbar-brand">Insurance app</a>
        <button class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#nav">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="nav" aria-controls="nav" aria-label="Expand Navigation">
          <ul class="navbar-nav">
            <li class="nav-item">
              <a href="http://localhost:3000" class="nav-link" aria-current="page">Home</a>
            </li>
            <li class="nav-item">
              <a href="http://localhost:3000/about.html" class="nav-link" aria-current="page">About</a>
            </li>
          </ul>
        </div>
      </div>
    </nav>
    <div class="container">
      <h2>Edit insurance</h2>
      <form id="form">
        <div class="row">
          <div class="col">
            <select id="select_type" class="form-control" value="<%= client.insurance.type %>">
              <!-- TODO Remake this into a dynamically generated list instead of hardcoded options -->
              <option value="Car" <% if (insurance.type === 'Car') { %>selected<% } %>>Car</option>
              <option value="Life" <% if (insurance.type === 'Life') { %>selected<% } %>>Life</option>
            </select>
          </div>
          <div class="col">
            <input type="number" class="form-control" placeholder="Amount" id="amount" value="<%= insurance.amount %>">
          </div>
        </div>
        <div class="row">
          <div class="col">
            <label for="start_date">Start Date</label>
            <input type="date" class="form-control" id="start_date" value="<%= insurance.start_date.toISOString().split('T')[0] %>">
          </div>
          <div class="col">
            <label for="end_date">End Date</label>
            <input type="date" class="form-control" id="end_date" value="<%= insurance.end_date.toISOString().split('T')[0] %>">
          </div>
        </div>
        <button class="btn btn-primary" id="update" type="button" data-clientid="<%= client._id %>" data-insuranceid="<%= insurance._id %>">Update</button>
        <a href="<%='http://localhost:3000/clients/' + client._id %>" class="btn btn-danger">Back</a>
      </form>
    </div>
  </body>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz"
    crossorigin="anonymous"></script>
  <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
    <script type="module" src="http://localhost:3000/js/edit_insurance.js"></script>
</html>
